{% extends '/admin/admin_base.html.twig' %}
{% block contenu %}
    <div>
        <h1>{{ app.request.get('_route') == "app_admin_artiste_ajouter" ? 'Ajouter' : 'Modifier'}} un artiste</h1>
        {{ form_start(form) }}
        <div class="row">
            <div class="col-4">
                {{ form_row(form.nom) }}
            </div>
            <div class="col-4">
                {{ form_row(form.style) }}
            </div>
        </div>
        {% if app.request.get('_route') == 'app_admin_artiste_ajouter' %}
            <button class="btn btn-primary" type="submit">Ajouter</button>
        {% else %}
            <button class="btn btn-primary" type="submit">Modifier</button>
        {% endif %}

        {{ form_end(form) }}
    </div>

{% endblock %}